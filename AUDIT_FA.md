# گزارش بازبینی سایت — v3.1 reviewed

تاریخ: ۴ سپتامبر ۲۰۲۶

## محدوده

منبع بررسی، فایل پیوست `asalarpour-academic-site-v3-final.zip` بود. نسخهٔ اصلاح‌شده در پوشه‌ای جدا ساخته شد و هیچ تغییری در repository یا سایت عمومی کاربر اعمال نشد.

آخرین پاسخ دریافت‌شده از دامنهٔ عمومی هنوز Jekyll/Minimal Light بود، هرچند خبرهای BMVC و IROS در آن وجود داشت. بنابراین نتیجهٔ آزمون مرورگر در این گزارش مربوط به بستهٔ جدید محلی است، نه تأیید استقرار آن در دامنهٔ عمومی.

منابع عمومی بررسی‌شده:
- https://asalarpour.github.io/
- https://github.com/asalarpour/asalarpour.github.io

## ایراد اصلی موبایل

در نسخهٔ v3، قانون دسکتاپِ دیرتر در CSS قانون موبایل را بازنویسی می‌کرد. شبکهٔ `.publication-compact .publication-body` به `90px minmax(0,1fr)` برمی‌گشت، درحالی‌که متن‌ها همچنان در ستون اول بودند. نتیجه در صفحهٔ ۳۹۰ پیکسلی: عنوان، نویسندگان و لینک‌ها در ستونی فقط ۹۰ پیکسل عرض نمایش داده می‌شدند.

این ایراد اسکرول افقی ایجاد نمی‌کرد؛ پس آزمون صرفاً «نداشتن overflow» برای کشف آن کافی نبود.

در نسخهٔ اصلاح‌شده عرض عنوان ۳۶۲ پیکسل است؛ یعنی کل عرض محتوای همان viewport. قوانین نهایی موبایل برای مقاله‌ها و پروژه‌ها تک‌ستونه شدند. علاوه بر overflow، عرض واقعی محتوای مقاله نیز بررسی شد.

## اندازه‌های ثبت‌شده در Chromium

| جزء | v3 دسکتاپ 1440 | v3.1 دسکتاپ 1440 | v3.1 موبایل 390 |
|---|---:|---:|---:|
| نام | 36px | 32px | 28px |
| تیتر بخش | 30.08px | 24px | 22px |
| معرفی | 15.04px | 16px | 16px |
| توضیح پژوهش | 13.28px | 15px | 15px |
| لینک حرفه‌ای | 12.32px | 14px | 14px |
| نویسندگان | 12.8px | 14px | 14px |
| نام محل انتشار | 12.32px | 14px | 14px |

شعار بزرگ Building perception از نسخهٔ v3 حذف شده بود و در نسخهٔ اصلاحی نیز وجود ندارد. تغییر این نسخه، کوچک‌کردن نام و تیتر و هم‌زمان خواناتر کردن متن عادی است، نه کوچک‌کردن تمام نوشته‌ها.

## اصلاحات محتوایی و نگهداری

- خبرهایی با عنوان کوتاه می‌توانند `link_text` داشته باشند؛ در صورت وجود لینک مقاله، نبودن عنوان کامل باعث از دست رفتن لینک نمی‌شود.
- صفحهٔ اصلی به‌جای اولین پروژهٔ ثابت، پروژه‌های `featured: true` را تا حد تنظیم‌شده نشان می‌دهد. نبودن پروژه باعث خطای ساخت نمی‌شود.
- لینک‌های سایت/کد/دادهٔ پروژه نمایش داده می‌شوند.
- حد تعداد مقاله، خبر و پروژهٔ خانه در `data/site.json` است؛ مقدار فعلی مقاله ۶ است.
- CV منو و affiliation در metadata از پروفایل خوانده می‌شوند.
- preprint دستهٔ مستقل دارد؛ Enter در فرم مقاله‌ها جست‌وجو را پاک نمی‌کند.
- خروجی build نمی‌تواند یک پوشهٔ غیرخالی و نامرتبط را بی‌اجازه پاک کند. برای build خارج از ریشه، پوشهٔ جدید یا خروجی قبلی دارای نشانگر سازنده استفاده شود.

## آزمون‌ها و نتیجه

- موتور: Chromium 144.0.7559.96 روی Linux.
- ۲۹ ترکیب صفحه/عرض بررسی شد: خانه در 320، 360، 390، 680، 768، 900، 901، 1024، 1366، 1440، 1920؛ شش صفحهٔ داخلی در 360، 768، 1440.
- در این ۲۹ حالت اسکرول افقی مشاهده نشد. این به‌تنهایی معیار کامل زیبایی یا دسترس‌پذیری نیست.
- جست‌وجوی GATE: یک نتیجه؛ Workshop: پنج نتیجه؛ Reset: سی‌وسه نتیجه؛ نمایش حالت بدون نتیجه موفق.
- Enter واقعی در کادر جست‌وجو نیز آزمایش شد: عبارت GATE و نتیجه حفظ شدند.
- تشخیص تم تاریک سیستم، تغییر دستی تم، بازشدن منوی موبایل و بسته‌شدن با Escape موفق.
- با JavaScript خاموش، لینک‌های منو قابل دسترسی‌اند؛ کنترل تم بی‌اثر پنهان است؛ حالت تاریک سیستم از CSS اعمال می‌شود.
- خطای JavaScript در آزمون‌های اجراشده مشاهده نشد.
- ده تست استاندارد Python برای رفتار سازنده موفق شدند. داده‌های آزمایشی مستقل از تعداد پروژه‌ها و مقاله‌های واقعی‌اند تا تغییر عادی محتوا این تست‌ها را بی‌دلیل خراب نکند.
- ساخت و بررسی محلی: هفت صفحهٔ HTML، سی‌وسه مقاله، بدون مرجع محلی مفقود و بدون تصویر فاقد alt.
- فایل‌های JSON مقاله‌ها نسبت به ورودی v3 بدون تغییر بایتی باقی ماندند.

## روش رندر و محدودیت‌های آزمون

برای رندر، HTML واقعی خروجی همراه با CSS/JavaScript خود سایت در Chromium قرار گرفت؛ تصاویر محلی به دادهٔ داخلی تبدیل شدند. این روش درخواست شبکه ندارد. تصاویر پیش‌نمایش، رندر همین خروجی‌اند، نه تصویرسازی هوش مصنوعی.

این محیط، origin واقعی HTTP(S) برای آزمون نداشت. بنابراین ذخیره و بازیابی تم پس از بازدید مجدد در localStorage به‌صورت انتها‌به‌انتها تست نشده است؛ وجود کد آن بررسی شد. استقرار واقعی GitHub Actions، رفتار شبکه، cache، زمان بارگذاری اینترنت و آزمون native Safari/Firefox/iOS انجام نشده‌اند. آزمون‌های بالا را نباید تضمین عملکرد روی تمام مرورگرها یا گواهی کامل دسترس‌پذیری دانست.

## لینک‌ها و داده‌های مقاله

شناسه‌های حرفه‌ای از پروفایل حفظ شدند:
- Scholar: https://scholar.google.com/citations?user=PPyF4hEAAAAJ&hl=en
- GitHub: https://github.com/asalarpour
- LinkedIn: https://www.linkedin.com/in/asalarpour/
- Email: mailto:asalarp@clemson.edu
- CV: /assets/files/Amir_Salarpour_CV.pdf

نتایج جست‌وجوی عمومی Scholar و LinkedIn با نام/دانشگاه تطبیق داشتند. دریافت مستقیم آن دو سرویس محدود شد؛ پس صحت شناسه را با تضمین دسترسی شبکه اشتباه نکنید. فایل محلی CV موجود است؛ تازگی محتویات CV در این بررسی ممیزی نشده است.

۱۷ رکورد مقاله از دادهٔ ورودی لینک خروجی ندارند. لینک حدسی به آن‌ها اضافه نشده و هیچ ادعایی مبنی بر آزمون تمام لینک‌های خارجی یا بازتأیید پذیرش هر ۳۳ مقاله مطرح نمی‌شود. هدف این بسته اصلاح نمایش و نگهداری است، نه تغییر سوابق پژوهشی.

## معماری واقعی و موارد تکمیل‌نشده

این پروژه Astro نیست؛ سازندهٔ Python بدون وابستگی خارجی، دادهٔ JSON و خروجی HTML/CSS/JS است. مرورگر برای دیدن محتوا به Python یا سرور برنامه نیاز ندارد.

پروژه‌ها فعلاً در Research فهرست می‌شوند؛ صفحهٔ اختصاصی خودکار برای هر پروژه، پنل مدیریت، صفحات مستقل دانشجویان/گرنت‌ها و CMS پیاده‌سازی نشده‌اند. این‌ها قابلیت توسعهٔ بعدی‌اند، نه قابلیت آمادهٔ این بسته.

CSS هنوز بخشی از لایه‌های تاریخی نسخه‌های قبلی را دارد. تضادهای شناخته‌شدهٔ این بررسی اصلاح شدند؛ این نسخه بازنویسی کامل stylesheet نیست.

فایل `DESIGN_BENCHMARK_FA.md` از نسخهٔ قبلی یک فهرست ۴۰ نام برای الهام طراحی داشت، نه گزارش قابل‌بازتولید اندازه‌گیری CSS آن ۴۰ سایت. در این بازبینی چنین اندازه‌گیری‌ای انجام نشده و ادعای قبلی تکرار نمی‌شود.

## پرونده‌های آزمون

خروجی خام در `audit/v3-measurements.json` و `audit/v31-measurements.json` است. این فایل‌ها دادهٔ محلی آزمون هستند و برای تولید یا میزبانی سایت لازم نیستند.
